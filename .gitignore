# Coursework text stays off the public repo.
# Anything public gets crawled and indexed, and a similarity checker will later
# match the submitted essay against this repository as an "internet source".
docs/
